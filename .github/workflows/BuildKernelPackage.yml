name: Build Kernel Package
on: [push, pull_request, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3
    - name: Build In Docker
      if: ${{ !contains(github.event.head_commit.message, '[no build]') }}
      run: |
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        BUILDUSER=renakoamaori
        BUILDGROUP=quintet
        HOSTNAME=There-s-No-Freaking-Way-I-ll-Be-Your-Lover-Unless
        cat << EOF > entrypoint.sh
        cd /build
        groupadd -r $BUILDGROUP
        useradd $BUILDUSER -g $BUILDGROUP -m
        passwd -d $BUILDUSER
        pacman -Syu --noconfirm --needed sudo base-devel git python-yaml
        printf '$BUILDUSER ALL=(ALL) ALL\\n' | tee -a /etc/sudoers
        chown -R $BUILDUSER:$BUILDGROUP ./
        sudo -u $BUILDUSER gpg --keyserver keyserver.ubuntu.com --recv-keys 38DBBDC86092693E 83BC8889351B5DEBBB68416EB8AC08600F108CDF
        sudo -u $BUILDUSER bash -c 'export MAKEFLAGS=j\$(nproc) && makepkg -s --noconfirm'
        EOF

        docker run -h $HOSTNAME -t -v $PWD:/build archlinux /bin/bash /build/entrypoint.sh

    - name: Restore Workspace Permissions
      if: ${{ !contains(github.event.head_commit.message, '[no build]') }}
      run: |
        sudo chown -R $USER:$USER .
    
    - name: Print sha512sums
      if: ${{ !contains(github.event.head_commit.message, '[no build]') }}
      run: sha512sum *.pkg.tar.*

    - name: Upload Built Packages
      if: ${{ !contains(github.event.head_commit.message, '[no build]') }}
      uses: actions/upload-artifact@v4
      with:
        name: built-packages
        path: ${{ github.workspace }}/*.pkg.tar.*
        
    - name: Create Tag
      if: ${{ !contains(github.event.head_commit.message, '[no rel]') && !contains(github.event.head_commit.message, '[no build]') && github.event_name != 'pull_request' }}
      id: create_tag
      run: |
        source PKGBUILD
        echo "tag=${pkgver}" >> $GITHUB_OUTPUT
        echo $pkgver $pkgrel
    - name: Release
      if: ${{ !contains(github.event.head_commit.message, '[no rel]') && !contains(github.event.head_commit.message, '[no build]') && github.event_name != 'pull_request' }}
      uses: softprops/action-gh-release@v1
      with:
         files: |
           ${{ github.workspace }}/*.zip
           ${{ github.workspace }}/*.pkg.tar.*
         tag_name: v${{ steps.create_tag.outputs.tag }}
         name: ${{ steps.create_tag.outputs.tag }} - Release Watanare
         draft: ${{ contains(github.event.head_commit.message, '[draft]') }}
         prerelease: ${{ contains(github.event.head_commit.message, '[prerel]') }}
         body: |
           Fork release — linux-t2 with the BCM4377 D3-ACK suspend quirk (T2 Macs: MacBookPro16,3 / A2289 · MacBookPro15,4 / A2159 · MacBookAir9,1 / A2179). Everything upstream linux-t2 is, plus a working system suspend on affected boards.

           This release features Renako Amaori from There's No Freaking Way I'll Be Your Lover! Unless... — every major release features a new anime/manga, minor releases different characters!
           Install packages with `sudo pacman -U <file>`, you can use urls or file paths.
      env:
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Tailscale
      if: ${{ !contains(github.event.head_commit.message, '[no rel]') && !contains(github.event.head_commit.message, '[no build]') && github.event_name != 'pull_request' }}
      uses: tailscale/github-action@v4
      with:
        oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
        audience: ${{ secrets.TS_AUDIENCE }}
        tags: tag:ci
        version: latest

    - name: Upload and Update Repo
      if: ${{ !contains(github.event.head_commit.message, '[no rel]') && !contains(github.event.head_commit.message, '[no build]') && github.event_name != 'pull_request' }}
      env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_USER: ${{ secrets.SSH_USER }}
        SSH_KEY: ${{ secrets.SSH_KEY }}
        REMOTE_DIR: ${{ secrets.REMOTE_DIR }}
        REPO_NAME: ${{ secrets.REPO_NAME }}
      run: |
        source PKGBUILD
        # Setup SSH
        mkdir -p ~/.ssh
        echo "$SSH_KEY" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts

        # Upload Packages
        PKG_FILES="${pkgbase}*-${pkgver}-${pkgrel}-x86_64.pkg.tar.zst"
        scp -i ~/.ssh/id_ed25519 $PKG_FILES $SSH_USER@$SSH_HOST:$REMOTE_DIR/

        # Update Repository
        ssh -i ~/.ssh/id_ed25519 $SSH_USER@$SSH_HOST "cd $REMOTE_DIR && repo-add $REPO_NAME.db.tar.zst $PKG_FILES"
